{
  "name": "quick-bytes",
  "version": "1.0.0",
  "private": true,
  "description": "Interview-prep notes on AI models and LLMs — with interactive diagrams and OKF knowledge bundle.",
  "scripts": {
    "manifest": "node scripts/build-manifest.js",
    "dev": "npm run manifest && tailwindcss -i css/main.css -o css/tailwind.css --watch",
    "build": "npm run manifest && tailwindcss -i css/main.css -o css/tailwind.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
